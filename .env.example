# Database — matches docker-compose.yml exactly

DB_HOST=localhost

DB_PORT=5432

DB_NAME=geodb

DB_USER=gis_admin

DB_PASSWORD=ChangeMe123!

# Local data paths

DATA_RAW_DIR=./data/raw

DATA_OUTPUT_DIR=./data/output

# Optional: Census Bureau API key (free at api.census.gov/data/signup.html)

# Required only for ACS demographic data fetch

CENSUS_API_KEY=

